# semantic.toml - what this nest's data *means* (RFC-0016). Read by the MCP `schema`
# tool, the admin UI, and the scaffolded skill. Edit descriptions freely; the
# `[table.*.footguns]` are DERIVED from the ABI and regenerated - leave them be.

schema_version = 1

[nest]
description = "Load-bearing World Chain index for the active Turing Swap vault: SwapVM fills, identity-priced fees, risk policy, and LP cash flows."

[table.active_vault__liquidity_added]
description = "`LiquidityAdded(address,address,uint256,uint256,uint256)` events emitted by the `active_vault` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LiquidityAdded(address,address,uint256,uint256,uint256) event"

[table.active_vault__liquidity_added.columns]
amount0 = "The `amount0` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amount1 = "The `amount1` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
provider = "The `provider` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
receiver = "The `receiver` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
shares = "The `shares` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.active_vault__liquidity_added.footguns]
big_ints = [
    "amount0",
    "amount1",
    "shares",
]
overflows_dec = [
    "amount0",
    "amount1",
    "shares",
]

[table.active_vault__liquidity_removed]
description = "`LiquidityRemoved(address,address,uint256,uint256,uint256)` events emitted by the `active_vault` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LiquidityRemoved(address,address,uint256,uint256,uint256) event"

[table.active_vault__liquidity_removed.columns]
amount0 = "The `amount0` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amount1 = "The `amount1` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
provider = "The `provider` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
receiver = "The `receiver` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
shares = "The `shares` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.active_vault__liquidity_removed.footguns]
big_ints = [
    "amount0",
    "amount1",
    "shares",
]
overflows_dec = [
    "amount0",
    "amount1",
    "shares",
]

[table.vault_quota__fee_schedule_updated]
description = "`FeeScheduleUpdated(address,uint256,uint256,uint256,uint256,uint256)` events emitted by the `vault_quota` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FeeScheduleUpdated(address,uint256,uint256,uint256,uint256,uint256) event"

[table.vault_quota__fee_schedule_updated.columns]
humanShareBps = "The `humanShareBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tightFeeBps = "The `tightFeeBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tightVolume = "The `tightVolume` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
token = "The `token` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
wideFeeBps = "The `wideFeeBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
wideVolume = "The `wideVolume` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.vault_quota__fee_schedule_updated.footguns]
big_ints = [
    "tightFeeBps",
    "wideFeeBps",
    "tightVolume",
    "wideVolume",
    "humanShareBps",
]
overflows_dec = [
    "tightFeeBps",
    "wideFeeBps",
    "tightVolume",
    "wideVolume",
    "humanShareBps",
]

[table.vault_quota__risk_policy_applied]
description = "`RiskPolicyApplied(address,uint256,bytes32,uint256,uint256,uint256,uint256)` events emitted by the `vault_quota` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per RiskPolicyApplied(address,uint256,bytes32,uint256,uint256,uint256,uint256) event"

[table.vault_quota__risk_policy_applied.columns]
decisionHash = "The `decisionHash` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
desiredTightFeeBps = "The `desiredTightFeeBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
indexedThroughBlock = "The `indexedThroughBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
riskSpreadBps = "The `riskSpreadBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tightFeeBps = "The `tightFeeBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
token = "The `token` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
wideFeeBps = "The `wideFeeBps` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.vault_quota__risk_policy_applied.footguns]
big_ints = [
    "indexedThroughBlock",
    "desiredTightFeeBps",
    "riskSpreadBps",
    "tightFeeBps",
    "wideFeeBps",
]
overflows_dec = [
    "indexedThroughBlock",
    "desiredTightFeeBps",
    "riskSpreadBps",
    "tightFeeBps",
    "wideFeeBps",
]

[table.vault_quota__usage_recorded]
description = "`UsageRecorded(uint256,address,uint256,uint256)` events emitted by the `vault_quota` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UsageRecorded(uint256,address,uint256,uint256) event"

[table.vault_quota__usage_recorded.columns]
amount = "The `amount` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
day = "The `day` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
humanId = "The `humanId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
token = "The `token` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.vault_quota__usage_recorded.footguns]
big_ints = [
    "humanId",
    "amount",
    "day",
]
overflows_dec = [
    "humanId",
    "amount",
    "day",
]

[table.vault_router__human_gated]
description = "`HumanGated(bytes32,address,uint256,bool,uint256)` events emitted by the `vault_router` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per HumanGated(bytes32,address,uint256,bool,uint256) event"

[table.vault_router__human_gated.columns]
feeE9 = "The `feeE9` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
humanId = "The `humanId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderHash = "The `orderHash` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
taker = "The `taker` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tight = "The `tight` bool parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.vault_router__human_gated.footguns]
big_ints = [
    "humanId",
    "feeE9",
]
overflows_dec = [
    "humanId",
    "feeE9",
]

[table.vault_router__swapped]
description = "`Swapped(bytes32,address,address,address,address,uint256,uint256)` events emitted by the `vault_router` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per Swapped(bytes32,address,address,address,address,uint256,uint256) event"

[table.vault_router__swapped.columns]
amountIn = "The `amountIn` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountOut = "The `amountOut` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maker = "The `maker` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderHash = "The `orderHash` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
taker = "The `taker` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tokenIn = "The `tokenIn` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tokenOut = "The `tokenOut` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.vault_router__swapped.footguns]
big_ints = [
    "amountIn",
    "amountOut",
]
overflows_dec = [
    "amountIn",
    "amountOut",
]

[view.turing_activity_mix]
description = "Volume-weighted World ID-verified and searcher activity derived from mined SwapVM fills."

[view.turing_all_trades]
description = "The canonical active-vault trade feed consumed by the API, charts, and strategist."

[view.turing_fee_history]
description = "Executable verified-retail and searcher fee schedules emitted by the active vault controller."

[view.turing_risk_window]
description = "The latest 50 active-vault fills normalized into tETH notional for the guarded fee strategist."

[view.turing_vault_trades]
description = "HumanGated and Swapped events correlated into one auditable row per active-vault fill."
