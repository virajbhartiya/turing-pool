{
  "name": "turing-pool",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.2.2",
  "description": "An Aqua AMM that prices bounded, sybil-resistant human-backed order flow. ETHGlobal Lisbon 2026.",
  "scripts": {
    "build": "pnpm --filter @turing-pool/web build && pnpm --filter @turing-pool/server build",
    "test": "pnpm --filter @turing-pool/economics test && pnpm --filter @turing-pool/server test && pnpm --filter @turing-pool/agent test && pnpm --filter @turing-pool/subgraph build && (cd contracts && forge test)",
    "typecheck": "pnpm --filter @turing-pool/web typecheck && pnpm --filter @turing-pool/server typecheck && pnpm --filter @turing-pool/agent typecheck",
    "format:check": "cd contracts && forge fmt --check",
    "check": "pnpm typecheck && pnpm test && pnpm format:check",
    "e2e": "bash scripts/e2e.sh",
    "e2e:fork": "bash scripts/e2e.sh fork",
    "demo": "bash scripts/demo.sh local",
    "demo:fork": "bash scripts/demo.sh fork",
    "demo:sepolia": "bash scripts/demo-sepolia.sh",
    "demo:beats": "bash scripts/demo-beats.sh",
    "dev:web": "pnpm --filter @turing-pool/web dev",
    "test:vercel-runtime": "vercel build --yes && node --test test/vercel-runtime.test.mjs"
  },
  "dependencies": {
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2"
  }
}
