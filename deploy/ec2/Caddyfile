{$PUBLIC_DOMAIN} {
	encode zstd gzip

	reverse_proxy turing-pool-app:4021

	header {
		Referrer-Policy "strict-origin-when-cross-origin"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "SAMEORIGIN"
	}
}
